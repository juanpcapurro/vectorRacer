import vectorracer.Lobby

model{
    List<Lobby> lobbyList
}
json{
    "lobby-skeletons" lobbyList,{Lobby currentLobby ->
        id currentLobby.getId()
        name currentLobby.getName()
        size currentLobby.getPlayerList().size()
    }
}

